import React from "react";
import { Hero, HeroCard } from "../../components";

const HomePage = () => {
    return (
        <div>
            <Hero />
            <HeroCard />
        </div>
    );
};

export default HomePage;
